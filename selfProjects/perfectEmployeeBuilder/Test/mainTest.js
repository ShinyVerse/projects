const assert = require('chai').assert;
const toTest = require('../main');

describe('printing to screen the value', function(){
    it('should return value', function(){
        assert.equals(toTest.printTotalToScreen(30000, '£30000', 'done');
    });
});
